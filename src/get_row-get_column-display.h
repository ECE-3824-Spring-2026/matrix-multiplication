#ifndef GET_ROW_GET_COLUMN_DISPLAY_H
#define GET_ROW_GET_COLUMN_DISPLAY_H

#include "matrix.h"

/*
 * This header provides access to functionality implemented in
 * get_row-get_column-display.cpp WITHOUT including the .cpp file.
 *
 * NOTE:
 * - The Matrix class is already declared in matrix.h
 * - We do NOT declare main() here
 * - This prevents multiple-main linker errors
 */

// Optional: only declare this if you actually call it
void test_rank();

#endif
